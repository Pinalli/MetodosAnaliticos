# MetodosAnaliticos

## Congruent Linear Method for generating pseudo-random numbers, Python program that generates 1,000 (thousand) numbers (between the values ​​0 and 1) in order to produce a scatter plot
